/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RStudiewijzer } from '../../models/r-studiewijzer';

export interface StudiewijzersGet$Params {
  Range?: string;
}

export function studiewijzersGet(http: HttpClient, rootUrl: string, params?: StudiewijzersGet$Params, context?: HttpContext): Observable<StrictHttpResponse<{
'items'?: Array<RStudiewijzer>;
}>> {
  const rb = new RequestBuilder(rootUrl, studiewijzersGet.PATH, 'get');
  if (params) {
    rb.header('Range', params.Range, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<{
      'items'?: Array<RStudiewijzer>;
      }>;
    })
  );
}

studiewijzersGet.PATH = '/studiewijzers';
