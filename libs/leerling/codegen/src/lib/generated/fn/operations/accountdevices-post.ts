/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RAccountDevice } from '../../models/r-account-device';

export interface AccountdevicesPost$Params {
  
    /**
     * Request body
     */
    body: {
'items'?: Array<RAccountDevice>;
}
}

export function accountdevicesPost(http: HttpClient, rootUrl: string, params: AccountdevicesPost$Params, context?: HttpContext): Observable<StrictHttpResponse<{
'items'?: Array<RAccountDevice>;
}>> {
  const rb = new RequestBuilder(rootUrl, accountdevicesPost.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<{
      'items'?: Array<RAccountDevice>;
      }>;
    })
  );
}

accountdevicesPost.PATH = '/accountdevices';
