/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RAbsentieMeldingInvoer } from '../../models/r-absentie-melding-invoer';

export interface AbsentiemeldingenPost$Params {
  
    /**
     * Request body
     */
    body: {
'items'?: Array<RAbsentieMeldingInvoer>;
}
}

export function absentiemeldingenPost(http: HttpClient, rootUrl: string, params: AbsentiemeldingenPost$Params, context?: HttpContext): Observable<StrictHttpResponse<{
'items'?: Array<RAbsentieMeldingInvoer>;
}>> {
  const rb = new RequestBuilder(rootUrl, absentiemeldingenPost.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<{
      'items'?: Array<RAbsentieMeldingInvoer>;
      }>;
    })
  );
}

absentiemeldingenPost.PATH = '/absentiemeldingen';
