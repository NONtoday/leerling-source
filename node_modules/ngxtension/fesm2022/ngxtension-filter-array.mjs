import { map } from 'rxjs';

const filterArray = (filterFn) => map((array) => array.filter((item, index) => filterFn(item, index)));

/**
 * Generated bundle index. Do not edit.
 */

export { filterArray };
